let url;
export const prod = import.meta.env.PROD;

// if (prod) {
// 	url = 'https://signs.rcastellotti.dev/api';
// } else {
url = 'https://signs-api.rcastellotti.dev/api';
// }
export const apiUrl = url;
